# ProxyForge Release Checklist

This checklist tracks the desktop proof needed before a Linux or Windows build can be called Production Ready. A package is not release-ready just because `electron-builder` produced a file; each lane needs build, install, runtime, and security smoke evidence.

## Alpha Publication Gates

Source alpha status is limited to `0.1.0-alpha.1` source archives and GitHub tags. Create or publish the source alpha tag only after the GitHub source gate installs Chromium with `npx playwright install --with-deps chromium` and records `npm run test:ci:fast` passing for the exact commit. If a local extracted archive cannot download Playwright Chromium, record that as environment-blocked; it does not replace the required GitHub source-gate receipt.

Binary builds are allowed before certification. Treat AppImage, deb, NSIS, and portable outputs as unsigned validation artifacts until the exact release tag has native Linux and Windows artifacts, packaged Electron fuse receipts, packaged-license receipts, native release-smoke receipts, live-validation evidence, `release/SHA256SUMS.txt`, and a passing `npm run release:preflight`. Do not label binaries as public release-certified installers before this preflight passes.

## Build Commands

```bash
npm run test:release
npm run docs:pages
npm run test:platform-release
npm run test:ui-scale-production
npm run test:platform-shell-production
npm run test:agent-control-production
npm run test:ai-provider-production
npm run test:release-security-production
npm run test:fast-regression-production
npm run test:full-nightly-production
npm run test:full-nightly-history
npm run test:install-docs-production
npm run test:windows-package-production
npm run test:linux-package-production
npm run release:fuses
npm run test:release:fuses
npm run test:ci:fast
npm run test:ci:full
npm run test:project-import-compatibility
npm run test:security-review
npm run test:browser-launcher
npm run test:release:browser-routing
npm run test:proxy-history
npm run test:websocket
npm run dist:linux
npm run dist:win
npm run dist:win:zip
npm run release:smoke:linux
npm run release:smoke:windows
npm run release:live:source
npm run release:live:windows
npm run release:preflight
```

`npm run test:release` compiles the renderer and Electron runtime, then verifies the release scripts, packaged entrypoints, builder targets, ignored artifact directories, and this checklist.

`npm run release:fuses` verifies the source-side Electron fuse policy is wired into Electron Builder through `scripts/electron-fuse-policy.mjs`. `npm run test:release:fuses` verifies the policy itself: `NODE_OPTIONS`, inspector CLI arguments, and extra `file://` privileges are disabled; cookie encryption, embedded app.asar integrity validation, and app.asar-only loading are enabled; and the temporary `RunAsNode` compatibility exception is explicit. Native release jobs must additionally run `node scripts/electron-fuse-policy.mjs --verify --app <packaged Electron executable>` against the exact Linux or Windows artifact.

`npm run release:preflight` is the final public-publish guard. Run it only after live validation and the native Linux/Windows artifact matrix have completed for the exact release tag. It requires `test-results/live-validation-summary.json` with `releaseDecision: GO`, present live-validation artifact paths under `test-results/`, Windows live-validation evidence and log output, the release tag peeled to the checked-out commit, Linux and Windows Electron fuse receipts, packaged-license receipts, release-smoke receipts, all four distributable artifacts, and `release/SHA256SUMS.txt` entries whose SHA-256 values match the actual artifacts.

`npm run release:live:source` runs the local authorized live-validation workflow through the real `proxyforge-agent` interface before public publish. It starts a loopback HTTP/HTTPS fixture, drives ProxyForge MITM capture through a real proxy listener, imports captured evidence, executes original and mutated replay traffic, runs scoped active scanner checks, validates local HTTP/DNS/SMTP OAST callbacks, exports JSON/Markdown reports, verifies report redaction of `pf_live_validation_token` and `pf_live_validation_secret`, and writes `test-results/live-validation-summary.json` plus the `test-results/live-*` evidence directories. `npm run release:live:windows` runs the same headless live workflow on a Windows node and writes `test-results/windows-live-validation/`; GUI/RDP smoke still needs to be recorded separately when required for final publish evidence.

`npm run test:platform-release` compiles the app and validates `proxyforge-platform-release-parity-evidence-package`, including dense React/Vite navigation, visual QA, Linux/Windows Electron shell launch, Linux and Windows package artifacts, packaged headless and agent CLIs, packaged runtime proxy/cert/OAST/report smokes, packaged browser routing, Linux install/trust/uninstall proof, Windows installer/uninstall proof, the `windows-trust-runner` trust-store pin, fast/full release gates, install/operator docs, release security review, full-fidelity executor material, and report-export-only redaction.

`npm run test:ui-scale-production` compiles the app and validates `proxyforge-ui-scale-production-evidence-package`, including desktop/tablet/mobile responsive proof, all major analyst surfaces, zero overlap and horizontal overflow, long-label fitting, keyboard and accessible-name reachability, stable fixed controls, large-project data density, bounded row windows, latency budgets, report attachment scale, packaged app.asar mode, full-fidelity executor material, and report-export-only redaction.

`npm run test:platform-shell-production` compiles the app and validates `proxyforge-platform-shell-production-evidence-package`, including Linux and Windows packaged Electron shell launch, structured `PROXYFORGE_RELEASE_SMOKE=1` payloads, packaged headless and agent CLIs, external-cwd app.asar agent execution for `~/vantix` style use, packaged runtime proxy/cert/OAST/report smokes, packaged browser routing, Linux and Windows package production gates, accepted trust-store and host-limit pins, full-fidelity executor material, and report-export-only redaction.

`npm run test:agent-control-production` compiles the app and validates `proxyforge-agent-control-production-evidence-package`, including the current 70-command source and packaged Codex/Claude/Vantix command surface, packaged app.asar status, external-cwd `~/vantix` invocation, persistent MITM, Chromium/cookie/data collection, Project Store recovery/backup, search/view/Sequencer/Decoder, replay/live-target profiling/WebSocket/Intruder/Repeater race/desync, insertion-point extraction, Scanner/Anvil/OAST promotion, extension/callback provider host proof/exploit/report, automation service lifecycle and installed-host service smoke/Vantix handoff, scope/approval/rate-limit/audit gates, long-running soak evidence, full-fidelity executor material, and report-export-only redaction.

`npm run test:ai-provider-production` compiles the app and validates `proxyforge-ai-provider-production-evidence-package`, including Codex CLI, Claude CLI, OpenAI-compatible `/v1/chat/completions` HTTP/local providers, provider config persistence, CLI diversity, streaming telemetry, token/cost accounting, prompt templates, baselines, comparisons, benchmark replay, controlled action packages, scope blocking, no direct provider action traffic, package-refresh evidence, long-run profiling, docs/schema coverage, full-fidelity executor prompt material, and report-export-only redaction.

`npm run test:release-security-production` compiles the app and validates `proxyforge-release-security-production-evidence-package`, including the formal zero-warning release security review, local listener bindings, report-phase review/export redaction, full-fidelity operational secret preservation, exploit controls, 70-command agent controls, AI provider controls, signed trust, artifact hygiene, Windows trust-store pin acceptance, Linux clean-runtime proof, production CI/operator-doc gates, package-refresh evidence, and report-export-only redaction.

`npm run test:fast-regression-production` compiles the app and validates `proxyforge-fast-regression-production-evidence-package`, including required fast-suite step names, required step kinds, zero failed steps, broad release/platform/security/project/agent/browser/proxy/scanner/repeater/intruder/report coverage, `test-results/ci-fast-suite-summary.json` artifact retention, full-fidelity executor material, and report-export-only redaction.

`npm run test:full-nightly-production` compiles the app and validates `proxyforge-full-nightly-production-evidence-package`, including the scheduled full/nightly plan, production gate coverage, coverage owners, upload and retention policy, scheduled retained-history restore/save continuity, zero-flake budget, current plan-only boundary, retained trend dashboard, historical full-run pass, latest runtime pass, fast-suite linkage, safe artifact paths, full-fidelity executor material, and report-export-only redaction.

`npm run test:full-nightly-history` compiles the app and validates `proxyforge-full-nightly-retained-history-evidence-package` plus `proxyforge-full-nightly-hosted-retained-history-evidence-package`, including retained runtime summaries, `test-results/ci-full-suite-history/dashboard.json`, current runtime summary linkage, plan-only exclusion from runtime history, hosted scheduled run receipts, branch/workflow continuity, retained-history restore/save, uploaded summary/dashboard artifacts, full and skip-browser run counts, digest integrity, zero-flake policy, safe artifact paths, full-fidelity executor material, and report-export-only redaction.

`npm run test:install-docs-production` compiles the app and validates `proxyforge-install-docs-production-evidence-package`, including packaged Linux/Windows install guide, operator guide, release checklist, release evidence, Codex/Claude/Vantix agent docs, release smoke commands, certificate trust, browser routing, DPAPI and Windows trust-store pin coverage, replay/desync/race/scanner/exploit/OAST workflows, troubleshooting, production signoff, full-fidelity executor material, and report-export-only redaction.

`npm run test:windows-package-production` compiles the app and validates `proxyforge-windows-package-production-evidence-package`, including native Windows NSIS, portable, and win-unpacked artifacts, Windows zip fallback hygiene, unpacked and installed GUI launch, installed headless scan/report, installed runtime proxy/cert/OAST/report, browser routing, DPAPI sample-cookie extraction, quiet uninstall, `windows-trust-runner` trust-store pin acceptance, portable-wrapper stdout pinning, full-fidelity executor material, and report-export-only redaction.

`npm run test:linux-package-production` compiles the app and validates `proxyforge-linux-package-production-evidence-package`, including AppImage, deb, and linux-unpacked artifacts, AppImage and unpacked runtime/GUI smokes, deb metadata/dependency coverage, packaged headless scan/report, packaged agent and external-cwd invocation, packaged runtime proxy/cert/OAST/report, packaged browser routing, clean-container deb install/runtime/GUI/trusted-CA/uninstall proof, known warning pins, full-fidelity executor material, and report-export-only redaction.

`npm run test:ci:fast` is the curated fast regression gate for every feature checkpoint. It builds once, runs release readiness plus representative Browser, Platform/Release, UI Scale production, Platform Shell production, Agent Control production, AI Provider production, Release Security production, Fast Regression production, Full/Nightly production, Full/Nightly retained history, Install Docs production, Windows Package production, Linux Package production, Project, Safety/Enterprise, Search, Automations, Extensions, Callback, signed tenant-isolated OAST relay integration, external OAST provider diversity, Proxy, Scanner, Repeater, Intruder, and Headless engine/runtime checks, validates the nightly full-suite workflow policy, then runs focused browser workflow smokes for Search, Automations, and Extensions. Fast Regression production evidence packages are part of the gate so the gate itself stays production-reviewable. UI Scale production evidence packages are part of the gate so responsive overflow, large-project usability, stable controls, latency budgets, and report attachment scale stay visible. Full/Nightly production evidence packages are part of the gate so the scheduled plan, retained trend dashboard, zero-flake budget, coverage owners, historical full-run pass, plan-only boundary, and artifact retention stay visible. Full/Nightly retained history evidence packages are part of the gate so per-run runtime summaries, `ci-full-suite-history/dashboard.json`, digest integrity, current-run linkage, and plan-only exclusion stay visible. Release Security production evidence packages are part of the gate so local listeners, operational secrets, exploit controls, agent/AI controls, platform pins, signed trust, production CI, artifact hygiene, package refresh, and report-only redaction stay visible. AI Provider production evidence packages are part of the gate so Codex CLI, Claude CLI, OpenAI-compatible HTTP/local providers, prompt benchmarks, controlled action packages, no-direct-action traffic, package-refresh proof, and report-only redaction stay visible. Agent Control production evidence packages are part of the gate so the current 70-command packaged agent contract, app.asar/external-cwd execution, active-workflow policy gates, live-target profiling, WebSocket agent operations, Project Store recovery/backup, Scanner OAST promotion, Automation service lifecycle plans, Automation installed-host service smoke, and long-running agent soaks stay visible. Platform Shell production evidence packages are part of the gate so Linux/Windows shell launch, app.asar/external-cwd agent execution, packaged runtime/browser proof, and known host-limit pins stay visible. Install Docs production evidence packages are part of the gate so the packaged install/operator/release/agent docs stay synchronized with the current commands, artifact paths, high-risk workflows, and full-fidelity executor secret boundary. Windows Package production evidence packages are part of the gate so native Windows package proof, installed runtime proof, browser/DPAPI proof, and the accepted trust-store pin remain visible. Linux Package production evidence packages are part of the gate so AppImage/deb/unpacked proof, clean-container trusted-CA proof, and known warning pins remain visible. Platform/Release parity evidence packages are part of the gate so dense UI QA, Linux/Windows shells, artifacts, packaged CLIs, runtime/browser smokes, docs, platform pins, security review, and full-fidelity executor material stay covered. Safety/Enterprise parity evidence packages are part of the gate so scope gates, throttles, request caps, approvals, override audit, signed governance, SSO federation fixtures, remote policy transport, backend soak, remote audit retention, and full-fidelity executor material stay covered. Project parity evidence packages are part of the gate so save/restore, `.proxyforge.json` migration, legacy proxy XML/HAR/JSONL/raw HTTP import interop, managed browser profiles, cookie readiness, and full-fidelity executor material stay covered. Project import compatibility evidence packages are part of the gate so large mixed legacy proxy XML/HAR/raw HTTP/JSONL/ProxyForge v1 corpora, duplicate detection, conflict preservation, parser diagnostics, package refresh proof, and full-fidelity executor material stay visible. Customer-scale interop evidence packages are part of the gate so 3,000-plus imported mixed-format exchanges, host/route diversity, Search/Viewer/Logger/Target scale, Repeater/Scanner/Intruder handoff, report attachment scale, Project Store backup/reopen budgets, and full-fidelity executor samples stay visible. Extension parity evidence packages are part of the gate so catalog/local manifests, signed manifests/updates, runtime diagnostics, evidence handoff, sandbox APIs, third-party SDK edge packages, and full-fidelity executor material stay covered. The Proxy history engine step writes `proxyforge-proxy-edge-profile-package` evidence covering listener capture, CONNECT, HTTPS MITM, intercept, match/replace, HTTP/2, cross-tool handoff, WebSocket, browser-proxy-chain proof, package-refresh digests, full-fidelity executor material, and report-export-only redaction. It also writes `proxyforge-proxy-browser-proxy-chain-diversity-package` evidence covering Chromium/Chrome/Edge/Firefox, Linux/Windows, upstream-auth/CONNECT chain/PAC/direct proxy modes, project/trusted/manual/pinned certificate modes, HTTP/2, CONNECT, WebSocket, isolated profile/cookie stores, preserved upstream credentials, package refresh, and report-export-only redaction. The Intruder engine step writes `proxyforge-intruder-live-target-profile-package` evidence covering live-target diversity, high-volume streaming, checkpoint/resume linkage, grep/extract triage, authz differentials, payload transforms, package-refresh digests, full-fidelity executor material, and report-export-only redaction. The runner writes `test-results/ci-fast-suite-summary.json` for CI artifact upload.

`npm run test:ci:full` is the full/nightly regression orchestrator. It builds once; runs release readiness, release smoke planning, CI policy, release runtime, Platform/Release, UI Scale production, Platform Shell production, Agent Control production, AI Provider production, Release Security production, Fast Regression production, Full/Nightly production, Full/Nightly retained history, Install Docs production, Windows Package production, Linux Package production, Safety/Enterprise, Project, Project import compatibility, customer-scale interop profiling, third-party Extension compatibility, Proxy listener/history/MITM/intercept/WebSocket, Scanner passive/active/retest/Anvil/live calibration, Repeater transport/workspace/desync/race, Intruder, Logger, Organizer, Viewer, Search, Automation, Extensions, Callback, Exploit, AI provider/action, report, headless, agent CLI, and other committed Electron/runtime engine checks; runs the full Playwright browser workflow file; writes `test-results/ci-full-suite-plan.json`, `test-results/ci-full-suite-summary.json`, `.last-run`, retained runtime summaries under `test-results/ci-full-suite-history/`, `test-results/ci-full-suite-history/dashboard.json`, and browser artifacts under `test-results/playwright-artifacts/` so browser cleanup cannot remove retained runtime history; and records coverage owners, artifact paths, upload retention, and a zero-flake default budget. Use `npm run test:ci:full -- --plan-only` to validate CI metadata without executing the suite, and `npm run test:ci:full -- --skip-browser` for engine-only nightly triage.

`npm run test:security-review` generates and validates the formal release security review gate for local listener bindings, report-phase secret redaction, exploit controls, agent-control coverage, packaged app.asar/external-cwd agent proof, explicit platform pins, safety policy, signed trust, production CI/operator-doc gates, and artifact hygiene.

`.github/workflows/nightly-full-suite.yml` schedules `npm run test:ci:full`, supports manual `plan_only` and `skip_browser` dispatch controls, provisions Chromium dependencies, opts JavaScript actions into the Node 24 runtime with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`, restores branch-local retained history with `actions/cache/restore@v4`, saves refreshed runtime history with `actions/cache/save@v4` unless the dispatch is plan-only, and uploads `test-results/ci-full-suite-summary.json`, `test-results/ci-full-suite-plan.json`, `.last-run`, `test-results/ci-full-suite-history/`, `test-results/ci-full-suite-history/dashboard.json`, `test-results/playwright-artifacts/`, JSON/Markdown evidence, and Playwright reports with 30-day retention for failed runs.

The same workflow also runs a change-time source gate on `pull_request` and protected `main` pushes. That gate builds the source, runs `release:fuses`, `test:release:fuses`, release readiness, agent option audit, CI policy checks, and a production dependency audit. A native artifact matrix runs on Linux and Windows for non-scheduled change events, builds the platform artifact with the package `dist:*` scripts, verifies Electron fuses on the packaged executable, runs `scripts/release-smoke.mjs`, and uploads the observed artifact receipts. These receipts are required for a public alpha; fixture or schema evidence alone must stay `productionReady: false`.

`npm run test:browser-launcher` validates Linux/Windows managed browser launch matrices, proxy-profile arguments, Firefox proxy/certificate preferences, and Chromium/Firefox cookie extraction fixtures.

`npm run test:release:browser-routing` is a host-aware production evidence smoke. On hosts with Chromium, Chrome, or Edge it launches an isolated profile through ProxyForge and verifies the routed request appears in Proxy history. Add `--browser-trust-store` through `scripts/release-smoke.mjs` on hosts with `certutil` / `libnss3-tools` to import the project CA into an isolated Chromium NSS trust store and prove HTTPS capture without `--ignore-certificate-errors`. Hosts without a Chromium-family browser should record the blocked result and continue with other proof lanes.

`npm run test:proxy-history` validates advanced Proxy history filters, HTTP/2-aware metadata, cross-tool handoff, the Proxy edge profile package, and the browser/proxy-chain diversity package, including saved predicate filter-set packages, annotation lane counts, raw sample preservation, operational secret signals, pseudo-headers, ALPN/h2c and stream hints, multiplexed authority buckets, trailers, fidelity warnings, Repeater raw requests, Scanner insertion/check hints, Reports attachment fingerprints, listener/CONNECT/MITM/intercept/match-replace/WebSocket/browser-proxy-chain package links, Chromium/Chrome/Edge/Firefox profile coverage, Linux/Windows profile coverage, upstream credential preservation, and report-ready evidence packages.

`node tests/report-engine.mjs` validates report export parity, `proxyforge-report-production-readiness-package`, `proxyforge-report-external-bundle-diversity-package`, and `proxyforge-report-template-library-interop-package`, including renderer comparison across Markdown/HTML/JSON/bundle/PDF HTML/fallback PDF/PDF QA metadata, local and external signed-bundle verification, four recipient/channel/key/template external shared-bundle profiles, template library export/import, duplicate conflict review, built-in/custom template rendering, digest-only no-secret review, JSON-stable signature canonicalization, semantic PDF accessibility/tag readiness, long-project report scale, large-PDF warnings, full-fidelity pre-export inputs, report-export redaction, and signed bundle tamper rejection.

`node tests/repeater-desync-race-engine.mjs` validates Repeater desync/race runtime hardening and `proxyforge-repeater-race-desync-production-package`, including parser-differential framing, socket-backed single-connection proof, last-byte race proof, single-packet race soak proof, scope blocking, release-skew/race-window budgets, response ordering, raw transcript preservation, operational secret preservation, package-refresh digests, and report-export-only redaction.

`node tests/logger-column-engine.mjs` validates Logger custom column parity evidence, including sandbox blocked-token diagnostics, request/response/entry helper APIs, cookie/content-type/path/json helpers, concatenated returns, compatibility fixtures, and large-table profile packages.

`node tests/organizer-evidence-engine.mjs` validates Organizer parity evidence, including multi-tool collections, notes/status/highlight coverage, reviewer assignment, due dates, reviewer SLA export, CSV/share links, plain and passphrase-sealed packages, HMAC signatures, trust policy review, import diff review, conflict/dedupe routes, all merge modes, conflict audit trails, full-fidelity raw material, operational passphrase/signing-secret preservation, and report-export-only redaction.

`node tests/project-parity-engine.mjs` validates Project parity evidence, including local save/restore, `.proxyforge.json` import/export, legacy schema migration into v1, legacy proxy XML/HAR/JSONL/raw HTTP import interop, selected session profile restore, managed Linux/Windows browser launch profiles, browser cookie extraction/decryption readiness, full-fidelity raw request/response and cookie/API-key preservation, and report-export-only redaction.

`node tests/project-import-compatibility-engine.mjs` validates `proxyforge-project-import-compatibility-evidence-package`, including large mixed legacy proxy XML, HAR, raw HTTP, JSONL, and ProxyForge v1 corpora; merge manifests; duplicate detection; conflict preservation; parser diagnostics; package refresh proof; full-fidelity raw request/response and cookie/API-key preservation; and report-export-only redaction.

`node tests/customer-scale-interop-engine.mjs` validates `proxyforge-project-customer-scale-interop-evidence-package`, including 3,000-plus imported mixed-format exchanges, 40-host and 3,000-route scale, duplicate/conflict diagnostics, Search/Viewer/Logger/Target profiling, Repeater/Scanner/Intruder handoff counts, report attachment scale, Project Store backup/reopen budgets, package refresh proof, full-fidelity raw request/response samples, operational secret samples, and report-export-only redaction. The same gate now emits `proxyforge-project-customer-workspace-restore-interop-package` with four imported customer workspace restore profiles, Project Store backup/reopen proof, restored-exchange integrity, cross-tool restore counts, full-fidelity raw samples, and report-export-only redaction.

`node tests/safety-enterprise-engine.mjs` validates Safety/Enterprise parity evidence, including scope gates, throttle floors, request caps, approval-required and approved active lanes, policy override/bypass audit, signed audit exports, governance packages, SSO identity mapping and federation fixtures, remote policy transport receipts, enterprise backend soak, remote audit retention, full-fidelity raw request/response and secret preservation, and report-export-only redaction.

`node tests/platform-release-engine.mjs` validates Platform/Release parity evidence, including dense UI navigation, visual QA, Linux and Windows shell smokes, Linux and Windows artifacts, packaged headless and agent CLIs, runtime proxy/cert/OAST/report smokes, packaged browser routing, Linux install/trust/uninstall proof, Windows installer/uninstall proof, explicit Windows trust-store pinning, fast/full release gates, install docs, release security review, full-fidelity raw material, operational secret preservation, and report-export-only redaction.

`node tests/ui-scale-production-engine.mjs` validates UI Scale production evidence, including desktop/tablet/mobile viewport proofs, all analyst surfaces, zero overlap or horizontal overflow, long-label text fitting, keyboard and accessible-name reachability, stable fixed controls, large-project table/window budgets, report attachment scale, packaged app.asar mode, full-fidelity raw material, operational secret preservation, and report-export-only redaction.

`node tests/platform-shell-production-engine.mjs` validates Platform Shell production evidence, including Linux and Windows packaged Electron shell launch, structured release-smoke payloads, packaged headless and agent CLIs, external-cwd app.asar agent execution, packaged runtime proxy/cert/OAST/report smokes, packaged browser routing, Linux and Windows package production gates, accepted trust-store and host-limit pins, full-fidelity raw material, operational secret preservation, and report-export-only redaction.

`node tests/agent-control-production-engine.mjs` validates Agent Control production evidence, including source and packaged 70-command status coverage, app.asar and external-cwd runtime proof, persistent MITM, Chromium/cookie/data collection, Project Store recovery/backup, search/view/Sequencer/Decoder, replay/live-target profiling/WebSocket/Intruder/Repeater race/desync, insertion-point extraction, Scanner/Anvil/OAST promotion, extension/callback provider host proof/exploit/report, automation service lifecycle and installed-host service smoke/Vantix, active workflow policy/audit gates, long-running soak packages, full-fidelity raw material, operational secret preservation, and report-export-only redaction.

`node tests/ai-provider-production-engine.mjs` validates AI Provider production evidence, including Codex CLI provider proof, Claude CLI provider proof, OpenAI-compatible HTTP/local provider interop, provider configuration persistence, streaming telemetry, token/cost accounting, prompt template libraries, baselines, comparisons, benchmark replay, controlled action packages, scope blocking, no direct action traffic from providers, package-refresh evidence, docs/schema coverage, full-fidelity operational prompt material, operational secret preservation, and report-export-only redaction.

`node tests/release-security-production-engine.mjs` validates Release Security production evidence, including the formal zero-warning security review package, all security-review categories, local listener binding, report-phase review/export redaction, full-fidelity operational secret preservation, exploit approvals/scope/rate/stop controls, 70-command agent controls, AI provider controls, platform pins, signed trust, production CI metadata, clean Linux/Windows runtime evidence, artifact hygiene, package refresh, docs/schema coverage, and report-export-only redaction.

`node tests/fast-regression-production-engine.mjs` validates Fast Regression production evidence, including the required 50-plus-step surface map, zero failed steps, release readiness, Platform/Release, Safety/Enterprise, Project, agentic control, browser workflow, Proxy, Scanner, Repeater, Intruder, Reports, release security review, summary artifact retention, full-fidelity operational material, operational secret preservation, and report-export-only redaction.

`node tests/install-docs-production-engine.mjs` validates Install Docs production evidence, including packaged install, operator, checklist, release-evidence, and agent docs; Linux and Windows smoke command coverage; certificate trust and browser routing coverage; DPAPI/trust-store pin coverage; Codex/Claude/Vantix agent operation; replay/desync/race/scanner/exploit/OAST workflows; recovery/troubleshooting; Production Ready signoff; full-fidelity operational material; and report-export-only redaction.

`node tests/windows-package-production-engine.mjs` validates Windows Package production evidence, including native Windows NSIS, portable, win-unpacked, and zip fallback artifacts; unpacked and installed GUI launch; installed headless scan/report; installed runtime proxy/cert/OAST/report; browser routing; DPAPI sample-cookie extraction; quiet uninstall; formal `windows-trust-runner` trust-store pin acceptance; portable-wrapper stdout pinning; full-fidelity operational material; and report-export-only redaction.

`node tests/linux-package-production-engine.mjs` validates Linux Package production evidence, including AppImage, deb, and linux-unpacked artifacts; AppImage and unpacked runtime/GUI smokes; deb metadata and dependency coverage; packaged headless scan/report; packaged agent and external-cwd invocation; packaged runtime proxy/cert/OAST/report; browser routing; clean-container deb install/runtime/GUI/trusted-CA/uninstall proof; known warning pins; full-fidelity operational material; and report-export-only redaction.

`node tests/scanner-passive-engine.mjs` validates Scanner passive checks and dedupe parity, including security headers, cookie flags, CORS, cache controls, mixed content, information disclosure, authz metadata, server errors, exact duplicates, route variants, confidence summaries, severity policy normalization, false-positive suppressions, report attachments, full-fidelity raw exchanges, operational secret signals, and report-export-only redaction.

`node tests/scanner-active-scan-engine.mjs` validates Scanner active scan evidence packaging and `proxyforge-scanner-live-target-profile-package`, including plan/review/matrix/replay linkages, active/suppressed finding status, report attachment manifests, CI/headless handoff, OAST promotion, Anvil compatibility, retest deltas, calibration soak linkage, target host/route/status diversity, check-family depth, package-refresh digests, full-fidelity raw request/response samples, operational secret signals, and report-export-only redaction.

`node tests/scanner-retest-engine.mjs` validates Scanner retest evidence deltas, including issue linkage, baseline proof, retest proof, fixed/regressed/still-vulnerable/inconclusive outcome coverage, request edits, runner controls, report attachments, full-fidelity raw samples, operational secret signals, and report-export-only redaction.

`node tests/anvil-engine.mjs` validates Anvil custom scan-check parity evidence, including plain-text `.anvil` definitions, reusable rule libraries, positive/negative fixtures, fixture validation, custom-only headless runs, signed package review, Scanner finding handoff, Reports attachments, full-fidelity raw samples, operational secret signals, and report-export-only redaction.

`node tests/extension-engine.mjs` validates Extension parity evidence packages, including catalog and local manifest coverage, enable/disable run logs, sandboxed request/response/editor/scanner/headless hooks, legacy proxy-compatible compatibility fixtures, third-party SDK edge packages, policy-denied operations, dependency review, headless CI evidence, signed manifests and updates, runtime diagnostics, evidence handoff, full-fidelity raw samples, operational secret signals, and report-export-only redaction.

`node tests/extension-third-party-compatibility-engine.mjs` validates `proxyforge-extension-third-party-sdk-compatibility-package`, including extension package/local/agent profile diversity, `IHttpRequestResponse` mutation and annotations, `IExtensionHelpers` transform coverage, multi-message context menus, session token refresh, scanner insertion points, editor/state lifecycle cleanup, fail-closed unsupported APIs, manifest/dependency migration edges, package refresh proof, full-fidelity operational secrets, and report-export-only redaction.

`node tests/intruder-engine.mjs` validates Intruder parity evidence, including attack-mode matrices, saved attack libraries, payload transformations, checkpoint/resume queues, resource-pool concurrency, grep/extract comparison, high-volume streaming, and `proxyforge-intruder-live-target-profile-package` coverage for live-target diversity, authz differential status classes, package-refresh linkage, full-fidelity operational material, and report-export-only redaction.

`npm run test:websocket` validates socket-backed WebSocket upgrade tunneling, bidirectional frame capture, binary/text payload accounting, intercept/edit/drop/replay paths, match/replace-style frame rewriting, saved replay payload preservation, state graph/filter/export metadata, connection clustering, JSON/Markdown transcript exports, restored and large-transcript import proof, and full-fidelity WebSocket capture plus intercept/rewrite/replay/state-transcript evidence packages with operational secret signals preserved until report export.

`node tests/active-scanner.mjs` validates the socket-backed Active Scanner runtime and check-pack parity evidence, including out-of-scope blocking, throttle/max-request controls, all built-in check families, supported/unsupported check-pack accounting, authz two-leg comparison, dedupe/confidence evidence, tuning metadata, full-fidelity raw probes, operational secret signals, and report-export-only redaction.

`node tests/crawl-audit.mjs` validates Crawl-derived Scanner insertion audit evidence, including query/form/path coverage, duplicate merge accounting, out-of-scope skip review, active Scanner handoff, scope/throttle/max-insertion controls, finding dedupe/confidence, full-fidelity raw probe samples, operational secret signals, and report-export-only redaction.

Current committed evidence is tracked in [RELEASE_EVIDENCE.md](RELEASE_EVIDENCE.md). The evidence log can prove a build checkpoint, but it does not replace clean-machine install and runtime smoke tests.

End-user install and first-run commands are documented in [INSTALL_LINUX_WINDOWS.md](INSTALL_LINUX_WINDOWS.md). Day-to-day capture, replay, scanner, OAST, reporting, recovery, troubleshooting, and agent workflows are documented in [OPERATOR_GUIDE.md](OPERATOR_GUIDE.md). `scripts/release-smoke.mjs` is the reusable smoke runner; it writes structured JSON into `test-results/` and supports plan-only checks with `node scripts/release-smoke.mjs --plan`.

## Linux Lane

Expected artifacts:

- `release/*.AppImage`
- `release/*.deb`

Install and runtime smoke:

- Launch the AppImage on a clean Linux desktop profile.
- Run `npm run release:smoke:linux` to prove the packaged app loads its renderer under Xvfb or an available desktop session.
- Run `node scripts/release-smoke.mjs --platform linux --artifact release/linux-unpacked/proxyforge --browser-routing --out test-results/release-smoke-linux-browser-routing.json` on a host with Chromium/Chrome/Edge to prove packaged browser routing.
- Add `--browser-trust-store` to that command on a host with `certutil` to prove isolated-browser project CA trust for HTTPS capture without `--ignore-certificate-errors`.
- Run `PROXYFORGE_DOCKER="sudo -n docker" scripts/release-deb-container-smoke.sh --gui --browser-trust` or the same script with plain Docker access to prove clean-container deb install, packaged runtime startup, installed GUI launch under Xvfb, isolated Chromium trusted-CA HTTPS capture, package query, uninstall, and installed executable removal.
- Install the deb package and confirm the desktop launcher opens ProxyForge.
- Start the proxy listener, route a browser through it, and confirm HTTP history capture.
- Generate and trust the project certificate authority, then confirm HTTPS capture and certificate trust behavior.
- Export a report bundle and confirm the file opens outside the app.
- Run `proxyforge headless --help` or the packaged equivalent and confirm the headless CLI starts.
- Run the packaged `proxyforge-agent` equivalent from `resources/app.asar/scripts/proxyforge-agent.mjs status --json` and confirm Codex/Claude/Vantix commands plus the full-fidelity executor secret boundary are exposed.
- Run the packaged headless scan/report smoke and confirm JSON/Markdown reports plus SARIF/JUnit artifacts are written under `test-results/release-smoke-headless/`.
- Run the packaged runtime workflow smoke and confirm proxy HTTP capture, HTTPS MITM with project CA, HTTP/DNS/SMTP OAST callbacks, JSON/Markdown/HTML/PDF/bundle report exports, operational-secret preservation, and report-phase redaction under `test-results/release-smoke-runtime/`.
- Run the packaged browser routing smoke and confirm isolated-profile Chromium/Chrome/Edge traffic is captured under `test-results/release-smoke-browser-routing/`.
- For unpacked artifacts, run the app with `ELECTRON_RUN_AS_NODE=1` against `resources/app.asar/dist-electron/headlessRunner.js --help`.

## Windows Lane

Expected artifacts:

- `release/*Setup*.exe` or another NSIS installer artifact.
- `release/*portable*.exe`
- `release/*-win.zip` as a Linux-host fallback artifact when Wine is unavailable. This proves a Windows x64 bundle can be assembled, but it does not replace the NSIS/portable installer smoke gate.

Install and runtime smoke:

- Install the NSIS build on a clean Windows profile.
- Launch the portable executable without installation.
- Run `npm run release:smoke:windows` on a Windows host so `release/win-unpacked/ProxyForge.exe` loads with `PROXYFORGE_RELEASE_SMOKE=1` and exits with a structured payload.
- Run `node scripts\release-smoke.mjs --platform windows --artifact "release\ProxyForge Setup 0.1.0-alpha.1.exe" --uninstall` to prove silent NSIS install, installed app launch, quiet uninstall, and installed executable removal.
- Add `--browser-routing --dpapi-cookie` on a Windows host with Edge or Chrome when collecting browser-routing and DPAPI sample-cookie proof from the packaged app.
- Add `--browser-trust-store` only on a Windows host/session that permits temporary `Cert:\CurrentUser\Root` mutation and cleanup. `windows-trust-runner` currently records this as a blocked, nonfailed lane with `ERROR_NOT_SUPPORTED`, so keep Windows OS/browser trust-store HTTPS capture pinned unless a different host lane is selected.
- Run the zip fallback inspection from non-Windows hosts only when NSIS/portable generation is not available yet.
- Start the proxy listener, route Edge or Chrome through it, and confirm HTTP history capture.
- Generate and trust the project certificate authority through the Windows trust workflow, then confirm HTTPS capture when the host permits temporary current-user Root store mutation; otherwise record the blocked trust-store proof and continue.
- Confirm DPAPI/browser cookie extraction behavior with `releaseCookieDpapiSmoke.js`, which creates a synthetic Chromium profile, wraps the profile key with the current Windows user's DPAPI, preserves full operational cookie output, and redacts cookie values from the summary.
- Export a report bundle and confirm the file opens outside the app.
- Run `proxyforge headless --help` or the packaged equivalent from PowerShell and confirm the headless CLI starts.
- Run the packaged `proxyforge-agent` equivalent from PowerShell and confirm the agentic command surface starts from the installed/unpacked app.
- The NSIS installer smoke must include the installed headless CLI, installed headless scan/report, and installed runtime proxy/cert/OAST/report checks before uninstalling; the Windows production pass must also include installed browser routing when the host browser lane is available.
- `windows-trust-runner` / `windows-trust-runner` can be used for Windows install/browser proof, but Windows deployment proof is pinned as nonblocking if that runner cannot provide the required OS/browser trust-store lane; continue production hardening on the remaining verifiable gates.

## Shared Release Gates

- Release artifacts stay in ignored `release/`; temporary local artifacts move to ignored `.gitignored/` instead of being deleted when progress would stall.
- Electron fuse verification is required for every packaged artifact. The current policy disables `NODE_OPTIONS`, inspector CLI arguments, and extra `file://` privileges; enables cookie encryption, embedded app.asar integrity validation, and app.asar-only loading; and records `RunAsNode` as a temporary compatibility exception until packaged CLI/release-smoke execution no longer needs `ELECTRON_RUN_AS_NODE`.
- The package contains `dist/**/*`, `dist-electron/**/*`, `LICENSE`, and `package.json`.
- The desktop entrypoint is `dist-electron/main.js`.
- The preload bridge is `dist-electron/preload.js`.
- The packaged CLI entrypoint is `dist-electron/headlessRunner.js`.
- `npm run release:trust` writes package-lock-derived SBOM, SHA-256 checksum, and provenance manifests under `test-results/release-trust/` for the exact source/build/docs/workflow files being promoted.
- Proxy, certificate, crawler, browser-launch, project snapshot, AI, report, and sequencer Electron engines compile into `dist-electron/`.
- The release smoke captures the app version, platform, artifact name, install result, proxy result, certificate trust result, browser launch result, headless result, agent CLI result, packaged external-cwd agent invocation, OAST result, DPAPI sample-cookie result, operational secret boundary, and report export result. Packaged headless report-export artifacts are retained under ignored `test-results/release-smoke-headless/`; packaged runtime workflow artifacts are retained under ignored `test-results/release-smoke-runtime/`; packaged browser-routing artifacts are retained under ignored `test-results/release-smoke-browser-routing/`; packaged external-cwd agent artifacts are retained under ignored `test-results/release-smoke-agent-external-cwd/`; packaged DPAPI sample-cookie artifacts are retained under ignored `test-results/release-smoke-dpapi-cookie/`.
- The first-run smoke mode uses `PROXYFORGE_RELEASE_SMOKE=1` so packaged desktop builds can load the renderer, print a `proxyforge-release-smoke` result, and exit without manual clicks.
- The curated fast regression suite passes and uploads `test-results/ci-fast-suite-summary.json` before packaging.
- The package license verifier passes in source mode and in native artifact mode: `node tests/release-package-license.mjs` must pass before packaging, and `npm run release:license -- --verify-artifacts` must pass against the built `release/*/resources/app.asar` payload before installer upload.
- The private output permission regression passes: `node tests/private-output-permissions.mjs` must prove existing user-selected output parents keep their modes while created files remain private.
- The Fast Regression production evidence package is current for the suite: `node tests/fast-regression-production-engine.mjs` must pass and `.gitignored/test-artifacts/fast-regression-production-engine/fast-regression-production-evidence-package.json` must be retained for handoff.
- The Full/Nightly production evidence package is current for the scheduled suite: `node tests/full-nightly-production-engine.mjs` must pass and `.gitignored/test-artifacts/full-nightly-production-engine/full-nightly-production-evidence-package.json` must be retained for handoff. The package must include a trend dashboard, historical full-run pass, latest runtime pass, zero-flake budget, and the explicit boundary that plan-only summaries validate metadata but do not claim runtime completion.
- The Full/Nightly retained history evidence packages are current for runtime trend retention: `node tests/full-nightly-history-engine.mjs` must pass and `.gitignored/test-artifacts/full-nightly-history-engine/full-nightly-retained-history-evidence-package.json` plus `.gitignored/test-artifacts/full-nightly-history-engine/full-nightly-hosted-retained-history-evidence-package.json` must be retained for handoff. The packages must include retained runtime summaries, hosted scheduled run receipts, `test-results/ci-full-suite-history/dashboard.json`, current-run linkage, branch/workflow continuity, retained-history restore/save, uploaded summary/dashboard artifacts, plan-only exclusion from runtime history, digest integrity, and report-export-only redaction.
- The UI Scale production evidence package is current for dense analyst UI scale: `node tests/ui-scale-production-engine.mjs` must pass and `.gitignored/test-artifacts/ui-scale-production-engine/ui-scale-production-evidence-package.json` must be retained for handoff. The package must include desktop/tablet/mobile overflow proof, all major surface reachability, long-label fitting, stable controls, large-project data density, bounded row windows, latency budgets, report attachment scale, packaged mode proof, and report-export-only redaction.
- The Project import compatibility evidence package is current for mixed-history interop: `node tests/project-import-compatibility-engine.mjs` must pass and `.gitignored/test-artifacts/project-import-compatibility/project-import-compatibility-evidence-package.json` must be retained for handoff. The package must include legacy proxy XML, HAR, raw HTTP, JSONL, and ProxyForge v1 import coverage, duplicate and conflict diagnostics, package refresh proof, full-fidelity raw material, and report-export-only redaction.
- The customer-scale interop evidence packages are current for larger imported workspaces: `node tests/customer-scale-interop-engine.mjs` must pass, `.gitignored/test-artifacts/customer-scale-interop/customer-scale-interop-evidence-package.json` must be retained for handoff, and `.gitignored/test-artifacts/customer-scale-interop/customer-workspace-restore-interop-package.json` must be retained for restored-workspace handoff. The packages must include 3,000-plus imported mixed-format exchanges, host/route diversity, Search/Viewer/Logger/Target scale, Repeater/Scanner/Intruder handoff counts, report attachment scale, Project Store backup/reopen budgets, four imported customer workspace restore profiles, restored-exchange integrity, package refresh proof, full-fidelity raw material, and report-export-only redaction.
- The third-party Extension SDK compatibility package is current for broader extension interop: `node tests/extension-third-party-compatibility-engine.mjs` must pass and `.gitignored/test-artifacts/extension-third-party-compatibility/extension-third-party-sdk-compatibility-package.json` must be retained for handoff. The package must include extension package/local/agent profile diversity, helper transforms, multi-message context menus, session token refresh, insertion points, editor/state lifecycle cleanup, fail-closed unsupported APIs, manifest/dependency edges, package refresh proof, full-fidelity raw material, and report-export-only redaction.
- The Install Docs production evidence package is current for the release docs: `node tests/install-docs-production-engine.mjs` must pass and `.gitignored/test-artifacts/install-docs-production-engine/install-docs-production-evidence-package.json` must be retained for handoff.
- The Windows Package production evidence package is current for the Windows lane: `node tests/windows-package-production-engine.mjs` must pass and `.gitignored/test-artifacts/windows-package-production-engine/windows-package-production-evidence-package.json` must be retained for handoff.
- The Linux Package production evidence package is current for the Linux lane: `node tests/linux-package-production-engine.mjs` must pass and `.gitignored/test-artifacts/linux-package-production-engine/linux-package-production-evidence-package.json` must be retained for handoff.
- The Platform Shell production evidence package is current for the desktop shell: `node tests/platform-shell-production-engine.mjs` must pass and `.gitignored/test-artifacts/platform-shell-production-engine/platform-shell-production-evidence-package.json` must be retained for handoff.
- The Agent Control production evidence package is current for the Codex/Claude/Vantix control plane: `node tests/agent-control-production-engine.mjs` must pass and `.gitignored/test-artifacts/agent-control-production-engine/agent-control-production-evidence-package.json` must be retained for handoff.
- The AI Provider production evidence package is current for Codex/Claude/local provider execution and controlled AI actions: `node tests/ai-provider-production-engine.mjs` must pass and `.gitignored/test-artifacts/ai-provider-production-engine/ai-provider-production-evidence-package.json` must be retained for handoff.
- The Release Security production evidence package is current for local listeners, operational secrets, exploit controls, agent/AI controls, platform pins, signed trust, production CI, clean runtime evidence, artifact hygiene, and report-only redaction: `node tests/release-security-production-engine.mjs` must pass and `.gitignored/test-artifacts/release-security-production-engine/release-security-production-evidence-package.json` must be retained for handoff.
- The Release Trust production evidence package is current for SBOM, dependency integrity, license/dependency review policy, source/build/docs/workflow SHA-256 checksums, SLSA-lite provenance, signing/notarization state, verification commands, artifact retention, and report-only redaction: `node tests/release-trust-production-engine.mjs` must pass and `.gitignored/test-artifacts/release-trust-production-engine/release-trust-production-evidence-package.json` must be retained for handoff.
- The Platform/Release parity package is current for the build: `node tests/platform-release-engine.mjs` must pass and `.gitignored/test-artifacts/platform-release-engine/platform-release-parity-evidence-package.json` must be retained for handoff.
- The full/nightly regression suite either passes in full or records a failed `test-results/ci-full-suite-summary.json` with coverage owner, artifact path, and flake-budget metadata for every executed step. `proxyforge-full-nightly-production-evidence-package` is the retained production proof for the scheduled plan, trend dashboard, historical full-run pass, fast-suite linkage, artifact retention, full-fidelity executor material, and report-export-only redaction.
- The formal release security review passes for local listener bindings, report-phase secret redaction, exploit controls, agent-control coverage, packaged app.asar/external-cwd agent proof, explicit platform pins, safety policy, signed trust, production CI/operator-doc gates, and artifact hygiene before any Production Ready release claim; `proxyforge-release-security-production-evidence-package` is the retained production proof for that gate.
- Release trust evidence passes for package-lock SBOM, SHA-256 checksums, SLSA-lite provenance, dependency/license review state, signing/notarization state pins, and artifact retention before any Production Ready release claim; `proxyforge-release-trust-production-evidence-package` is the retained production proof for that gate.
- The post-MVP agentic control interface exists before Production Ready: `proxyforge-agent` lets Codex CLI, Claude CLI, and `~/vantix/` agents drive persistent MITM logging, Chromium/data collection, proxy import, Project Store status/recovery/backup, scoped crawl/content discovery planning and execution, live target profiling, target access-control review and site-map comparison, Automation list/run/CI-export/scheduler/parity/service-plan/service-smoke workflows, `search-index --soak` semantic indexing and large-project evidence, scoped `search-index --provider-url --execute` live semantic provider invocation, search/viewing, Decoder chains, replay, bulk replay high-volume/concurrency soaks, WebSocket list/replay/fuzz/transcript export, Intruder high-volume/concurrency soaks, insertion-point extraction, scanner, Scanner OAST issue promotion, Anvil custom scan checks, extension compatibility fixtures, callback poll/provider-probe/relay-plan/relay-soak/retention-prune/replay, approved exploit, bundle, and report workflows through documented commands, full-fidelity operational JSON outputs, submission/report redaction, scope gates, approvals, rate limits, audit artifacts, and external-cwd invocation with runtime modules resolved from the ProxyForge app root. The packaged release smoke must require the full current 70-command source surface from `status`, including `automation-list`, `automation-run`, `automation-ci-export`, `automation-scheduler-tick`, `automation-parity-export`, `automation-service-plan`, `automation-service-smoke`, `sequencer-analyze`, `decoder-chain`, `project-store-status`, `project-store-recover`, `project-store-backup`, `live-target-profile`, `insertion-points`, `websocket-list`, `websocket-replay`, `websocket-fuzz`, `websocket-transcript-export`, `scanner-retest`, `scanner-evidence-export`, `scanner-oast-promote`, `callback-provider-probe`, `anvil-plan`, `anvil-run`, `anvil-package-export`, `extension-fixtures`, and packaged external-cwd invocation capabilities.
- The packaged operator guide must cover install/runtime proof, CA trust, proxy/browser routing, search/viewing, replay/desync/race, scanner/crawl, Collaborator/OAST, Exploit Lab approvals, report redaction, agentic operation, recovery, troubleshooting, and Production Ready signoff boundaries.

## Status Boundary

Current release readiness is **Parity Candidate with thirteen Production Ready release/platform/AI/agent gates**: the curated fast regression suite, Linux/Windows install docs, Windows package lane, Linux package lane, Platform Shell row, Release Security row, Release Trust row, Codex provider row, Claude provider row, OpenAI-compatible provider row, prompt benchmark row, autonomous action row, and Agent Control row are Production Ready, while scheduled retained full/nightly CI trend history still needs final production signoff. The full/nightly gate now has `proxyforge-full-nightly-production-evidence-package`, `proxyforge-full-nightly-retained-history-evidence-package`, and `proxyforge-full-nightly-hosted-retained-history-evidence-package` coverage for the scheduled plan, coverage owners, upload/retention policy, branch-local retained-history restore/save continuity, zero-flake budget, local retained dashboard history, hosted run receipts, scheduled-vs-manual boundaries, branch/workflow continuity, uploaded summary/dashboard artifacts, full-fidelity executor material, and report-export-only redaction. Local retained full/nightly proof now records 7 consecutive runtime passes with one true full 80/80 browser-backed pass and six skip-browser passes, including the current 82/82 Collaborator/OAST-refresh run; manual GitHub-hosted warmups `26442381257` and `26442689546` passed, but scheduled receipts still need to accumulate before the full/nightly row itself is called Production Ready. UI Scale, Project import compatibility, customer-scale interop, Release Security, Release Trust, AI Provider, Agent Control, Platform Shell, Linux package, and Windows package production evidence remain current production anchors, with the Windows trust-store browser-routing lane formally pinned as nonblocking for `windows-trust-runner`.
